server:
  port: 8081
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/devcave_transactional?useSSL=false
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: 'create-drop'
    show-sql: true
    open-in-view: false
